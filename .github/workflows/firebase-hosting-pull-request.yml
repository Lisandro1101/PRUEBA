# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # ⭐️ CORRECCIÓN: Añadimos los pasos para construir el proyecto de Next.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Usamos una versión de Node.js compatible
      - name: Install and Build (vidriera)
        run: npm ci && npm run build
        working-directory: ./vidriera # ❗️ Le decimos que trabaje dentro de la carpeta 'vidriera'
      # ✨ MEJORA: Añade el paso de construcción para el juego.
      - name: Install and Build (app-juego)
        # 🐛 CORRECCIÓN: Se elimina 'working-directory' si los archivos del juego están en la raíz.
        # Si están en otra carpeta, ajusta la ruta aquí.
        run: npm ci && npm run build
      # 🐛 CORRECCIÓN: El paso de despliegue estaba roto en dos líneas.
      - name: Deploy 'web-publica' to Firebase Hosting preview
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JUEGOS_CUMPLE }}
          # ✨ MEJORA: Añadir el projectId explícitamente para mayor claridad.
          projectId: juegos-cumple
          target: web-publica
      - name: Deploy 'app-juego' to Firebase Hosting preview
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JUEGOS_CUMPLE }}
          # 🐛 CORRECCIÓN: Es mejor añadir el projectId explícitamente.
          projectId: juegos-cumple
          target: app-juego
